#frist line in form ab|cd:
l1 = input("frist line in form ab|cd: ")
l2 = input("second line in form de|fg: ")
l3 = input("divider line in form --|--: ")
l4 = input("third line in form hi|jk: ")
l5 = input("fourth line in form lm|no: ")
print("  " + l1[0] + "   " + l1[1] + "     " + l2[0] + "   " + l2[1])
print("+" + "-"*7 + "+" + "-"*7 + "+")
print("  " + l3[0] + "   " + l3[1] + "     " + l4[0] + "   " + l4[1])
print("+" + "-"*7 + "+" + "-"*7 + "+")
print("    " + l5[0] + "     " + l5[1])
# This script takes user input to create a formatted display of characters in a grid-like structure.
# This script takes user input to create a formatted display of characters in a grid-like structure.
# Each line of input should be in the form "ab|cd", where 'a', 'b', 'c', and 'd' are single characters.
# The output arranges these characters into a structured format with borders.
# This script takes user input to create a formatted display of characters in a grid-like structure.
# Each line of input should be in the form "ab|cd", where 'a', 'b', 'c', and 'd' are single characters.
# The output arranges these characters into a structured format with borders.
# Each line of input should be in the form "ab|cd", where 'a', 'b', 'c', and 'd' are single characters.
# The output arranges these characters into a structured format with borders.
# Each line of input should be in the form "ab|cd", where 'a', 'b', 'c', and 'd' are single characters.
# The output arranges these characters into a structured format with borders.
